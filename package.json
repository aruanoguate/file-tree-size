{
  "name": "file-tree-size-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "npm --prefix packages/json-tree-size run compile && npm --prefix packages/xml-tree-size run compile && npm --prefix packages/tree-size-preview run compile",
    "watch": "npm --prefix packages/json-tree-size run watch",
    "test": "npm --prefix packages/json-tree-size run test",
    "test:core": "npm --prefix packages/tree-size-core run test",
    "test:preview": "npm --prefix packages/tree-size-preview run test",
    "test:all": "npm --prefix packages/tree-size-core run test && npm --prefix packages/json-tree-size run test && npm --prefix packages/xml-tree-size run test && npm --prefix packages/tree-size-preview run test",
    "test:stress:json": "npm --prefix packages/json-tree-size run test:stress",
    "test:stress:xml": "npm --prefix packages/xml-tree-size run test:stress",
    "preview:build": "npm --prefix packages/tree-size-preview run compile",
    "preview:watch": "npm --prefix packages/tree-size-preview run watch",
    "preview:serve": "npm --prefix packages/tree-size-preview run serve",
    "release:json:patch": "node scripts/release.js json patch",
    "release:json:minor": "node scripts/release.js json minor",
    "release:json:major": "node scripts/release.js json major",
    "release:xml:patch": "node scripts/release.js xml patch",
    "release:xml:minor": "node scripts/release.js xml minor",
    "release:xml:major": "node scripts/release.js xml major"
  }
}
